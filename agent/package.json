{
  "name": "@timelord/agent",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "go build -o bin/executor .",
    "watch": "vai go run *.go",
    "test": "go test ./...",
    "lint": "go fmt ./... && golangci-lint run || true"
  }
}
