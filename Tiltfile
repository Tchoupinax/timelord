docker_compose('docker-compose.yml')

dc_resource("pgweb", labels=["dev"])
dc_resource("postgres", labels=["dev"])
dc_resource("vault", labels=["dev"])

dc_resource("server", labels=["timelord"])
