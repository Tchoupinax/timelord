# timelord UI

## Environement variables

- `API_URL`: http endpoint of the API (http://localhost:9988)
- `TIMELORD_REMOTE_API`: production API used by `pnpm watch:front` (proxied through the dev server)
- `CI_COMMIT_SHA`:
